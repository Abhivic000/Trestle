# Copy to `.env.test` (same folder). Used by the browser tests (`pnpm test:e2e`),
# which run the web app with `vite --mode test`. Point it at the SEPARATE
# "trestle-test" Supabase project and the test API port.

VITE_API_URL=http://localhost:4001
VITE_SUPABASE_URL=https://your-test-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_replace_me
