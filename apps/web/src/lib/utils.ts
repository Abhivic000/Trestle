// Combines class names, letting later Tailwind classes override earlier
// conflicting ones. `cn` is shadcn/ui's official helper package, which the
// generated components in components/ui import directly.
export { cn } from 'cn';
